To compile -

    g++ -lm -fopenmp -O3 main.cc readTable.cc constructGraph.cc initialize.cc iterate.cc timer.cc

To run -

   ./a.out 2 data/georgiyData.Vertex.csv data/georgiyData.Edges.csv data/georgiyPattern.Vertex.csv data/georgiyPattern.Edges.csv >& result_file

The first parameter is the number of openmp threads ... 1, 2, 4, or 8. Data files are in data/. Program assumes
that edges are sorted by src id.  The data directory includes a copy of the output file generated when I ran the
program on my system using 1, 2, 4, and 8 threads.
